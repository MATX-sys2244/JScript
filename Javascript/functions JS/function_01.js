/*
 in this first file,  I created one function JavaScript very simple
 Neste primeiro arquivo, eu criei uma função JavaScript muito simples
*/
function elixir(a, y){

    return a * 2 + y 
    /*
     The [elixir function] => return (a => our parameter) multiplying with number 2, 
     then , the result add with (y => our other parameter)

     A [função elixir] => retorna (a => nosso parâmetro) multiplicando com o número 2,
     depois, o resultado soma com (y => nosso outro parâmetro)
    */
   
}
console.log(elixir(5,4)) // 14
/*
    specifying my 2 parameters (a and y) and print the result in my console.
    especificando meus 2 parâmetros (a e y) e imprimindo o resultado no meu console.
*/